# Copy to .env (do not commit .env). Never commit Clerk secret keys.

# Clerk — Dashboard → API Keys → Publishable key (starts with pk_test_ or pk_live_)
# Your dev instance host: sensible-boa-35.clerk.accounts.dev
VITE_CLERK_PUBLISHABLE_KEY=

# SkillMentor Spring Boot API (NOT Clerk). Local dev:
VITE_API_BASE_URL=http://localhost:8081
# Production example: https://your-api.onrender.com
